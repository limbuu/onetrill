# steps:
# - name: 'gcr.io/kaniko-project/executor:latest'
#   args: ['--destination=gcr.io/$PROJECT_ID/onetrill', '--cache=true']
# tags: ['cloud-builders-community']
# images:
# - 'gcr.io/$PROJECT_ID/onetrill'
steps:
- name: gcr.io/kaniko-project/executor:latest
  args: ['--destinatio=$_IMAGE_NAME:$_IMAGE_VERSION','--cache=true']
substitutions:
  _IMAGE_NAME: "gcr.io/$PROJECT_ID/built-with-kaniko"
  _IMAGE_VERSION: "v1"
options:
  substitution_option: 'ALLOW_LOOSE'
logsBucket: 'gs://codehub-cloudbuild-logs'